DATABASE_URL="file:./namaa.db"
AUDIO_STORAGE_PATH="./storage/audio"
VOICE_SAMPLES_PATH="./storage/voice-samples"
# The engine listens on a Unix socket at storage/run/engine.sock — no port.
# SAWTAK_ENGINE_SOCKET="/tmp/sawtak-engine.sock"   # override the socket path
# TTS_ENGINE_URL="http://host:port"                # talk to a TCP engine instead
